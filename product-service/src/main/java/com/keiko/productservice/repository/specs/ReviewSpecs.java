package com.keiko.productservice.repository.specs;

import com.keiko.productservice.entity.Review;
import org.springframework.data.jpa.domain.Specification;

public class ReviewSpecs {

    /*public static Specification<Review> isProductReviews () {
        return (root, query, builder) -> {
            builder.lessThan (root.get (Customer_.createdAt), date);
        };
    }*/
}
